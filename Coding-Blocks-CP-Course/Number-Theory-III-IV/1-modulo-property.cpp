// code is written by krishna_6431/mr_krishna
#include <bits/stdc++.h>
using namespace std;
/*
//very very important

(a + b) % m = ((a % m) + (b % m)) % m;
(a - b) % m = ((a % m) - (b % m) + m) % m;
(a * b) % m = ((a % m) * (b % m)) % m;

//slightly different from others
(a/b) % m = ((a % m)*(b-1 %m)) % m;
(b-1) = multiplicative modular inverse
*/

int main()
{

    return 0;
}