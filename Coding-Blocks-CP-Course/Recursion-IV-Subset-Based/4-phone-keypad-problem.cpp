// code is written by krishna_6431/mr_krishna
#include <bits/stdc++.h>
using namespace std;
char keypad[][10] = {"", "", "ABC", "DEF", "GHI", "JKL", "MNO", "PQRS", "TUV", "WXYZ"};

void generate_names(char *in, char *out, int i, int j)
{
    if ()
}

int main()
{
    int n;
    char arr[n];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
    char output[100];
    generate_names(arr, output, 0, 0);
    return 0;
}